# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'sparkpoll' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Firebase'
    pod 'Firebase/Database'
  # Pods for sparkpoll
    pod 'FirebaseUI', '~> 0.5'       # Pull in all Firebase UI features
  target 'sparkpollTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sparkpollUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'sparkpoll WatchKit App' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sparkpoll WatchKit App

end

target 'sparkpoll WatchKit Extension' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sparkpoll WatchKit Extension

end
